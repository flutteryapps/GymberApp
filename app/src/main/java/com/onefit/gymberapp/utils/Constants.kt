package com.onefit.gymberapp.utils

enum class DeviceDensity(val density: String) {
    LDPI("ldpi"), // 0.75
    MDPI("mdpi"), // 1.0
    HDPI("hdpi"), // 1.5
    XHDPI("xhdpi"), // 2.0
    XXHDPI("xxhdpi"), // 3.0
    XXXHDPI("xxxhdpi"), // 4.0
}